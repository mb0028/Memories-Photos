name: memories_photos
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0-beta1+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  system_theme: ^3.3.0
  blur: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
