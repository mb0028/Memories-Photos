name: memories_photos
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0-beta1+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  blur: ^4.0.2

  ftp_server: ^2.3.2
  ftpconnect: ^2.0.10
  ip_go: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
